name: gard_app
description: Quick inventory app
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  mobile_scanner: ^5.0.0
  permission_handler: ^11.3.1

flutter:
  uses-material-design: true
